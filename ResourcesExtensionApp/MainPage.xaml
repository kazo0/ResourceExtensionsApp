<Page x:Class="ResourcesExtensionApp.MainPage"
	  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	  xmlns:local="using:ResourcesExtensionApp"
	  Background="{ThemeResource BackgroundBrush}">
	<StackPanel HorizontalAlignment="Center"
				VerticalAlignment="Center"
				Spacing="20">
		<ToggleSwitch x:Name="ThemeToggle" HorizontalAlignment="Center">
			<ToggleSwitch.OffContent>
				<PathIcon Data="{StaticResource MoonIcon}" />
			</ToggleSwitch.OffContent>
			<ToggleSwitch.OnContent>
				<PathIcon Data="{StaticResource SunIcon}" />
			</ToggleSwitch.OnContent>
		</ToggleSwitch>
		<Button Style="{StaticResource FilledButtonStyle}"
				Width="200"
				Content="FilledButtonStyle" />
		<Button Style="{StaticResource MyCustomButtonStyle}"
				Width="200"
				Content="MyCustomButtonStyle" />
	</StackPanel>
</Page>
